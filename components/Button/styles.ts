import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    height: '100%',
    width: '100%',
  },
  button: {
    width: 138,
    height: 38,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 20,
  },
  buttonContent: {
    width: 138,
    height: 38,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 20,
  },
  textBtn: {
    color: '#FFF',
    fontSize: 14,
  },
});
