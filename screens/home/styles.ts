import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 20,
  },
  title: {
    color: '#fff',
    fontSize: 28,
    fontWeight: '700',
    textAlign: 'center',
    paddingTop: 50,
    paddingBottom: 20,
  },
});
